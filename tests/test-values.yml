imagePullSecrets:
- name: private-registry

# Allow gitlab runner to run unprotected branches
runners:
  protected: false

networkPolicies:
  enabled: true
  
# Cypress testing values
bbtests:
  enabled: true
